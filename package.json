{
  "name": "aws-lambda-dynamodbstream-ts",
  "description": "Template for AWS Lambda DynamoDBStream with TypeScript",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.27",
    "@types/node": "^12.0.12",
    "typescript": "^3.5.2"
  }
}
